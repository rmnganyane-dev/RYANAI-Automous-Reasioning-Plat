# RyanAI has no Python runtime or Python package dependencies.
# Frontend dependencies are declared in package.json and package-lock.json.
# Desktop dependencies are declared in src-tauri/Cargo.toml and Cargo.lock.
