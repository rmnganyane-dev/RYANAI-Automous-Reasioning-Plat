For all designs I ask you to make, have them be beautiful, not cookie-cutter. Make webpages that are fully featured and worthy of production.

By default, this template supports JSX syntax with Tailwind CSS classes, React hooks, and Lucide React for icons. Do not install other packages for UI themes, icons, or state management unless absolutely necessary or I request them. Use icons from lucide-react for logos.

Import project modules with the `@/` path alias, which maps to `src/` (e.g. `@/components/Foo` == `src/components/Foo`), instead of deep relative paths like `../../components/Foo`.

CRITICAL PROJECT CONTEXT & CONSTRAINTS:
1. You are working on "RyanAI" (Project Ryan), an autonomous reasoning engine named after Mukhethwa Ryan Ganyane. 
2. ISOLATION RULE: RyanAI is a strictly standalone application. Under NO circumstances should you generate code, integrations, or database relations that connect this project to the Top Spaza Network, VulaTrust, SAPS Integrated Case Management System, or Uncle Security Service. Keep the domain models entirely separate.
3. The backend utilizes LangGraph ReAct orchestration, Fastify, Docker, and PostgreSQL. Keep all architectural suggestions aligned with this stack.