services:
  - type: web
    name: ryanai-sovereign-nexus
    env: node
    buildCommand: npm install && npm run build
    startCommand: node mcp-server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 9090